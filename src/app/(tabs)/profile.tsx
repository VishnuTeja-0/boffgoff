import React from 'react';
import { View, Text, Image } from 'react-native';

const Profile = () => {
  return (
    <View className='text-2xl justify-center align-middle'>
      <Text className="text-2xl font-bold">Profile</Text>
    </View>
  );
};

export default Profile;